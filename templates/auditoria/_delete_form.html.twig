<form method="post" action="{{ path('app_auditoria_delete', {'id': auditorium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auditorium.id) }}">
    <button class="btn">Delete</button>
</form>

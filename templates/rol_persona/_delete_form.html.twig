<form method="post" action="{{ path('app_rol_persona_delete', {'id': rol_persona.id}) }}" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rol_persona.id) }}">
    <button class="btn">Eliminar</button>
</form>

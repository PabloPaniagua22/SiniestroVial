<form method="post" action="{{ path('app_vehiculo_siniestro_delete', {'id': vehiculo_siniestro.id}) }}" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehiculo_siniestro.id) }}">
    <button class="btn">Eliminar</button>
</form>

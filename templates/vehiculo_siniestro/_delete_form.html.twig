<form method="post" action="{{ path('app_vehiculo_siniestro_delete', {'id': vehiculo_siniestro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehiculo_siniestro.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Vehiculo Siniestro{% endblock %}

{% block body %}
    <h1>Vehiculo Siniestro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehiculo_siniestro.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vehiculo_siniestro_index') }}">Volver a la lista</a>

    <a href="{{ path('app_vehiculo_siniestro_edit', {'id': vehiculo_siniestro.id}) }}">Editar</a>

    {{ include('vehiculo_siniestro/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}VehiculoSiniestro{% endblock %}

{% block body %}
    <h1>VehiculoSiniestro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehiculo_siniestro.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vehiculo_siniestro_index') }}">back to list</a>

    <a href="{{ path('app_vehiculo_siniestro_edit', {'id': vehiculo_siniestro.id}) }}">edit</a>

    {{ include('vehiculo_siniestro/_delete_form.html.twig') }}
{% endblock %}

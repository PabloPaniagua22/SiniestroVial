<form method="post" action="{{ path('app_detalle_siniestro_delete', {'id': detalle_siniestro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_siniestro.id) }}">
    <button class="btn">Delete</button>
</form>
